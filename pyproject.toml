[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proto-migrate"
version = "0.1.0"
description = "Versioned message codec with forward migration"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["proto_migrate"]
